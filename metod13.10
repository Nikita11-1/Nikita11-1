№1
using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Threading.Channels;
using System.Linq;
using System.Diagnostics;

class Program
{
    static void Main(string[] args)
    {
        int a = Convert.ToInt32(Console.ReadLine()); 
        int b = Convert.ToInt32(Console.ReadLine());
        Console.WriteLine("Результат сложения: " + slozenie(a, b));
    }
    static int slozenie(int a, int b) 
    {
        return a + b;
    }
}
№2 
using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Threading.Channels;
using System.Linq;
using System.Diagnostics;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Напишите число, если число чётное выведет 1, если наоборот -1");
        int a = Convert.ToInt32(Console.ReadLine()); 
        Console.WriteLine(ChetNet(a));

    }
    static int ChetNet(int a) 
    {
        return a % 2 == 0 ? 1 : -1;

    }
}
№3
using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Threading.Channels;
using System.Linq;
using System.Diagnostics;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Напишите число от 1 до 10");
        int a = Convert.ToInt32(Console.ReadLine()); 
        Console.WriteLine(Factorial(a));

    }
    static int Factorial(int a)
    {
        if (a == 1 || a == 0)
            return 1;
        else
            return a * Factorial(a - 1);
    }
}
№4
using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Threading.Channels;
using System.Linq;
using System.Diagnostics;

class Program
{
    static void Main(string[] args)
    {
        string a = Convert.ToString("Меня зовут Никита");
        Console.WriteLine(Obrstring(a));


    }
    static string Obrstring(string a)
    {
        char[] stroka = a.ToCharArray();
        Array.Reverse(stroka);
        string result = new string(stroka);
        return result;

    }
}
№5
using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Threading.Channels;
using System.Linq;
using System.Diagnostics;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Введите строку:");
        string input = Console.ReadLine();

        if (Palindrome(input))
        {
            Console.WriteLine("Строка является палиндромом.");
        }
        else
        {
            Console.WriteLine("Строка не является палиндромом.");
        }


    }
    static bool Palindrome(string str)
    {
        string stroka = str.Replace(" ", "").ToLower();

        for (int i = 0; i < stroka.Length / 2; i++)
        {
            if (stroka[i] != stroka[stroka.Length - 1 - i])
            {
                return false;
            }
        }

        return true;
    }
}
№6
using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Threading.Channels;
using System.Linq;
using System.Diagnostics;
using static System.Runtime.InteropServices.JavaScript.JSType;

class Program
{
    static void Main(string[] args)
    {
        int[] numbers = { 1, 2, 3, 4, 5 }; 
        double average = Srednee(numbers);
        Console.WriteLine($"Среднее значение массива: {average}");


    }
    static double Srednee(int[] array)
    {
        double sum = 0;
        foreach (int number in array)
        {
            sum += number; 
        }
        return sum / array.Length;
    }
}
